const initialstate=0;
var initialsatet2=
export const  changeNumber =(state=initialstate,action)=>{

      switch(action.type){
        case "increment" :return state+1;
        case "decrement":return state-1;
        default :return state;
      }
}
export const ecommerce=(state=initialstate2,action)=>{
     switch(action.type)
     {
        case"add":
        return (
        
        )
     }
}
